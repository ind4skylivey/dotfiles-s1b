# Opt-in security overlay. Linked only with --profile security.
# Sourced from the portable zshrc when this file exists.
# Not for shared machines. Does not install tools.

alias burp='burpsuite'
alias zap='zaproxy'
