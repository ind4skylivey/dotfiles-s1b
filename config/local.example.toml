# Copy to config/local.toml (gitignored). Host-only overrides.
# Never put tokens, email, or private disk paths in the git tree.

[user]
# name = "il1v3y"

# Host-only shell overlay (not the dump). Example:
# ~/.config/zsh/local.zsh
# ~/.config/fish/local.fish

[paths]
# ollama_models = "~/Ollama"

[desktop]
# backend = "niri"   # niri | dwm | plasma
