# Gaming module

Independent `--profile gaming` (also in `full`). Portable MangoHud only.

Does **not** copy dump `steam-launch.sh` (Plasma KWin compositing, `DISPLAY=:0`).

```bash
./install.sh --dry-run --profile gaming
```
