#!/bin/sh
ip=$(ip -4 addr show tun0 2>/dev/null | grep inet | awk '{print $2}' | cut -d/ -f1)
if [ -n "$ip" ]; then
    echo "^c#00ff88^VPN^c#ffffff^ON^d^"
else
    echo "^c#ff3333^VPN^c#ffffff^OFF^d^"
fi
